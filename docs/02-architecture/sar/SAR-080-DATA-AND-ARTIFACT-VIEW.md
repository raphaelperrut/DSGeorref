# SAR-080 — Data and Artifact View

PostgreSQL/PostGIS é system of record; filesystem gerenciado guarda artifacts imutáveis. `SPEC-005` define descriptor, kinds, ArtifactSet manifest v2, validation profiles e publication receipts. A major 1 permanece apenas para leitura na pasta compatibility.
