# Baseline Validation

A baseline ativa é v2.8. `make verify` executa validações das Fases A–E, integridade de requisitos, DDD, ADRs, schemas, TaskEnvelopes, DAG e guardrails Python. O ZIP final é gerado somente após todos os comandos retornarem PASS.
