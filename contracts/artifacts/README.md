# Artifact Contract

Contratos executáveis de `SPEC-005`. O registry de kinds define os tipos admitidos; o descriptor descreve cada arquivo; o ArtifactSet manifest agrega e registra lineage; publication record prova a promoção atômica. A v1 do manifest foi preservada em `compatibility/` somente para leitura durante a janela declarada.
