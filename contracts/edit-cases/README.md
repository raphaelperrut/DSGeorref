# Edit Case Registry

Contratos executáveis de `SPEC-002`. `edit-case.schema.json` governa o aggregate corrente; o event schema preserva histórico append-only; o workflow YAML é a autoridade das transições. Aplicação de edição sempre cria nova Attempt e ResultSnapshot.
