# AIBackend Protocol

Contratos executáveis de `SPEC-003`. Um backend conforme implementa exatamente `supports()`, `estimate()`, `execute()`, `validate()` e `publish()` na ordem declarada. `publish()` publica somente candidates; aceitação científica continua externa e subordinada ao SGV.
