# Template Contract

Contratos executáveis de `SPEC-004`. Templates são dados versionados, não código. Apenas `LITERAL`, `MUSTACHE_SAFE` e `STRUCTURED` são admitidos. Avaliação arbitrária, rede, ambiente e includes dinâmicos são proibidos.
