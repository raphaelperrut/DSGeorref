# Suporte

Antes de solicitar suporte durante a fase documental, execute `make doctor` e anexe apenas relatório e stack trace sanitizados. A implementação futura seguirá a ADR-054 e produzirá `DiagnosticSupportBundle` local por allowlist. Não anexe imagens, coordenadas, credenciais, paths absolutos ou artifacts restritos sem autorização.
