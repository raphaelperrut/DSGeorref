name: foundation-ci

on:
  push:
  pull_request:

jobs:
  verify-foundation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python --version
      - run: make verify
      - run: git diff --check
